sudo: required

env:
  - IMAGE_NAME="golang:staging"

services:
- docker

script:
- curl https://raw.githubusercontent.com/GoogleCloudPlatform/runtimes-common/travis_test/structure_tests/ext_run.sh > ext_run.sh
- chmod +x ext_run.sh
- docker version
- docker build -t "$IMAGE_NAME" -q base/
- ./ext_run.sh -i "$IMAGE_NAME" -v --config test/test_config.json
